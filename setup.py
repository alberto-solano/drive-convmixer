from setuptools import setup, find_packages

setup(
    name='drive-convmixer',
    version='1.0.0',
    url='https://github.com/drive-convmixer.git',
    author_email='alsoca2@alumni.uv.es',
    packages=find_packages()
)
